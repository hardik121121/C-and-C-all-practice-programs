#include <stdio.h>
#include <math.h>
int main()
{
    float a,b,area;
    printf("enter the height of the triangle :");
    scanf("%f",&a);
    printf("enter the base of a triangle : ");
    scanf("%f",&b);
    area=(a*b)/2;
    printf("arae of  triangle : %f",area);
    return 0;
}
