#include <stdio.h>

int main()
{ 
    int a,b,c;
    printf("the first angle is a :");
    scanf("%d",&a);
    printf("the second angle is b :");
    scanf("%d",&b);
    c=180-(a+b);
    printf("the third angle is:%d",c);
 
}