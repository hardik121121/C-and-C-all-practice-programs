#include <stdio.h>
#include <math.h>

int main()
{ 
    int a,b,k;
    printf("enter the value of a :");
    scanf("%d",&a);
    printf("enter the value of b :");
    scanf("%d",&b);
    k=pow(a,b);
    printf("power :%d",k);
 
}